Pull in the https://github.com/symisc/ascii_art dependency using CPM. Link the dependency to MyApp.

Then integrate it into the source code for MyApp so that the CLI accepts a single arg defining a path to a jpg which is the rendered to the terminal using the ascii_art library.

Ensure the project compiles and the executable can render banner.jpg.
